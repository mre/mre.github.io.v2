<footer>
❤️ <a href="https://twitter.com/matthiasendler">Follow me on Twitter</a> and never miss a post again.<br />
🎁 <a href="https://tinyletter.com/mre">Subscribe to new articles</a><br />

{%- comment %}
What follows is an ultra clunky way to iterate over a list of social media links.
This would be much easier with an array slice function.
{%- endcomment %}
{%- if comments %}
💬 Comments available on
{%- for comment in comments %}
    {%- if forloop.length == forloop.index %}
    {%- if forloop.length > 1 %}
    and
    {%- endif %}
    {{ comment }}.
    {%- else %}
    {%- if forloop.first == forloop.index %}
    {{ comment }}
    {%- else %}
    , {{ comment }}
    {%- endif %}
    {%- endif %}
{%- endfor %}
{%- endif %}
</footer>
