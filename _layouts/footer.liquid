<footer>
    {%- include _layouts/subscribe.liquid %}
    <h3>Continue reading</h3>
    <ul>
        {% for post in posts limit:5 %}
        <li><a href="{{post.path}}">{{post.title}}</a></li>
        {% endfor %}
    </ul>
</footer>
